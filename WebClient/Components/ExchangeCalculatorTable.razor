@using Microsoft.AspNetCore.SignalR.Client
@inject IConfiguration configuration
@inject IJSRuntime _jsRuntime;

<MudTabs Elevation="2" Rounded="true" ApplyEffectsToContainer="true" PanelClass="pa-6">
    <MudTabPanel Text="Market">
        <ExchangeCalculatorBody HubConnection="HubConnection" IsMarketPrice="true">

        </ExchangeCalculatorBody>
    </MudTabPanel>
    <MudTabPanel Text="Limit">
        <ExchangeCalculatorBody HubConnection="HubConnection" IsMarketPrice="false">

        </ExchangeCalculatorBody>
    </MudTabPanel>
</MudTabs>


@code {
    [Parameter]
    public HubConnection HubConnection { get; set; }
}
